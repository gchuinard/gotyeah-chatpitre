# ---------------------------------------------------------------------------
# chatpitre — variables d'environnement (exemple)
# Copier ce fichier en .env et renseigner les valeurs.
# ---------------------------------------------------------------------------

# Base de données PostgreSQL.
# En dev : le service Postgres du docker-compose.yml.
DATABASE_URL=postgresql://chatpitre:chatpitre@localhost:5432/chatpitre

# Secret de signature des sessions (HMAC-SHA256), 32 caractères minimum.
# Générer avec : openssl rand -hex 32
AUTH_SECRET=remplacer-par-le-resultat-de-openssl-rand-hex-32

# Emails des administrateurs, séparés par des virgules (insensible à la casse).
# Tout compte dont l'email est listé ici dispose des droits admin.
ADMIN_EMAILS=admin@chatpitre.local

# URL de base de l'application.
BASE_URL=http://localhost:3000

# Environnement : development | production
NODE_ENV=development

# ---------------------------------------------------------------------------
# Télé-rendez-vous (visioconférence WebRTC) — relais TURN Cloudflare Realtime.
# Optionnel en local : sans ces clés, l'app retombe sur un STUN public (les
# appels 1:1 en LAN/localhost fonctionnent, mais pas le relais derrière un NAT
# restrictif). Créer une clé TURN dans le dashboard Cloudflare Realtime, puis
# renseigner l'id de clé et le token d'API ci-dessous.
# ---------------------------------------------------------------------------
CLOUDFLARE_TURN_KEY_ID=
CLOUDFLARE_TURN_KEY_API_TOKEN=

# ---------------------------------------------------------------------------
# Documents des chats (carnets de vaccination, certificats…).
# UPLOAD_DIR : répertoire de stockage des fichiers. En conteneur (docker
# compose), il est surchargé vers /data/uploads (volume persistant) ; en dev
# sur l'hôte, laisser un dossier local (gitignoré). MAX_UPLOAD_MB : taille
# maximale par fichier, en Mo (doit rester <= client_max_body_size côté NPM).
# ---------------------------------------------------------------------------
UPLOAD_DIR=./.uploads
MAX_UPLOAD_MB=20
